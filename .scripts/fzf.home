#!/usr/env/bin fish

begin
    string unescape $FZF_HOME_DIRS
    $find_cmd -a -d 1 -t d -- . $FZF_HOME_DIRS
end \
    | sort -ur \
    | string replace "$HOME/" '' \
    | $fzf_cmd --border-label ' Home ' \
    | read -l result
or return

source (type --path fzf.open-path) ~/$result
